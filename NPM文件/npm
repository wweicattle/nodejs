 //package.json
 // 你所知道的，包管理工具npm。下载一个包package.json的配置文件

 //2.里面有该包中所依赖的库；
    ^2.3.2 2(主版本号) 3(次版本号) 2(修订号)
    
 // 3. 所有的版本号比如  axios ^2.3.2   表示2主版本号不变，之后的y与z都是最新的版本
 // 4. 所有的版本号比如  axios ~2.3.2   表示2主版本号不变，次版本号 是不变 的，z是最新的版本

//5.npm i j就是生产时依赖的模块
